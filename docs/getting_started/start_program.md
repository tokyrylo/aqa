# Запуск программ в Python

## Запуск готового файла с Python

Обычно мы пишем код в файлах. Давай создадим свою первую программу:
1. Откройте блокнот (или IDE — VS Code/PyCharm).
2. Напишите:
```python
print("Hello! This is my first program")
```
3. Сохраните файл как test.py (важно: расширение .py).
Например, на рабочем столе.

Теперь запустим его через консоль:

- Перейдём на рабочий стол:

   - **Windows**:
```bash
cd Desktop
```
   - **macOS/Linux**:
```bash
cd ~/Desktop
```
- Запускаем:
   - **Windows**:
```bash
python test.py
```
   - **macOS/Linux**:
```bash
python3 test.py
```
- Компьютер напечатает:

		Привет! Это моя первая программа.

📌 Мини-задание для закрепления:  
1. Создай файл `math.py`.  
2. Напиши в нём:  

```python
   print(2 + 3)
   print(10 * 5)
   print("Сегодня я учу Python")
```
3. Запусти через консоль.

## Запуск Python в интерактивном режиме

Попробуем «поиграть» с Python прямо в консоли.

Введите:

```bash
python
```

Появится что-то вроде:

```bash
Python 3.11.6 (main, ...)
>>>
```

Три стрелки >>> — значит, мы внутри Python. Теперь можно писать команды!

Например:

```bash
print("Hello, world!")
```

Компьютер ответит:

```bash
Hello, world!
```

Чтобы выйти, напишите:

```bash
exit()
```
или нажмите **Ctrl + Z (Windows)** / **Ctrl + D (macOS/Linux)**.